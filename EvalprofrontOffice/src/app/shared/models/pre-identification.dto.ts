export interface PreIdentificationDto {
  situation: string;
  nomEntreprise: string;
  secteur: string;
  production: boolean;
  collecte: boolean;
  transformation: boolean;
  services: boolean;
  transitionComment: string;
}
